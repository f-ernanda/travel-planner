import { Meta, StoryObj } from '@storybook/react'
import TravelCardSkeleton from '.'

export default {
  title: 'TravelCardSkeleton',
  component: TravelCardSkeleton
} as Meta

export const Default: StoryObj = {}
