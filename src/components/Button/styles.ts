'use client'

import styled from 'styled-components'

export const Wrapper = styled.button`
  background: blue;
  border: 0;
  border-radius: 0.4rem;
  color: white;
  height: 4rem;
  padding: 0.8rem;
`
